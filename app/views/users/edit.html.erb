<h1> Update User Profile</h1>
<section id= "user-profile-edit-form">
  <%= form_tag profile_edit_path %>

      <%= label_tag :name %>
      <%= text_field_tag :name, "#{@user.name}"%>

      <%= label_tag :email %>
      <%= text_field_tag :email, "#{@user.email}" %>

      <%= submit_tag 'Update Profile' %>
</section>
